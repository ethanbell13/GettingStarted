#Write a program that prints all prime numbers. 
#(Note: if your programming language does not support arbitrary size numbers, printing all primes up to the largest number you can easily represent is fine too.)

# there is an infitie number of prime numbers... I'll just let the user pick a finite number to go up to.
def all_primes():
    n = input('Input an integer greater than 3: ')
    n = int(n)
    if isinstance(n,int) == False:
        return print('Please input a positive integer great than 3.')
    elif n < 4:
        return print('Please input a positive integer great than 3.')
    else:
        x = 4
        prime = [1,2,3]
        while (x) <= n:
            for i in range(x):
                if i == 0 or i == 1:
                    continue
                elif (x % i) == 0:
                    x += 1
                    break
                elif (i + 1) == x: 
                    prime.append(x)
                    x += 1
        return print(prime)
